# 🤖 Availability Checker Bot

บอทสำหรับตรวจสอบช่องว่างการจองและส่งแจ้งเตือนผ่าน Discord Webhook โดยใช้ GitHub Actions รันอัตโนมัติทุก 5 นาที

## 🚀 วิธีการตั้งค่า

### 1. Fork Repository นี้
- กด Fork ที่มุมขวาบน
- Clone ไปยัง GitHub account ของคุณ

### 2. ตั้งค่า Discord Webhook Secret
1. ไปที่ Repository Settings > Secrets and variables > Actions
2. กด "New repository secret"
3. ตั้งชื่อ: `DISCORD_WEBHOOK_URL`
4. ใส่ค่า: `https://discord.com/api/webhooks/1436424986675122298/wJpkFF-6Wve2Awut51sFL7XmFqY0vTmIiHRjQ1PgJ9ZDrEhWat1RK8F78EfQuKEzNZKN`

### 3. เปิดใช้งาน GitHub Actions
1. ไปที่แท็บ "Actions" ใน repository
2. กด "I understand my workflows, go ahead and enable them"

### 4. ปรับแต่งการตั้งค่า (ถ้าต้องการ)

#### แก้ไขวันที่ที่ต้องการข้าม
แก้ไขใน `notify.py` ส่วน `SKIP_DATES`:
```python
SKIP_DATES = {
    "2025-11-19",  # เพิ่มวันที่ที่ไม่ต้องการตรวจสอบ
    "2025-11-26",
    # ...
}
```

#### แก้ไขช่วงวันที่ตรวจสอบ
แก้ไขใน `notify.py`:
```python
start_date = "2025-11-15"  # วันที่เริ่มต้น
end_date = "2026-02-15"    # วันที่สิ้นสุด
```

#### แก้ไขความถี่ในการรัน
แก้ไขใน `.github/workflows/availability-checker.yml`:
```yaml
schedule:
  - cron: '*/5 * * * *'  # ทุก 5 นาที
  # - cron: '*/10 * * * *'  # ทุก 10 นาที
  # - cron: '0 * * * *'     # ทุกชั่วโมง
```

## 📁 ไฟล์ที่สำคัญ

- `notify.py` - โค้ดหลักของบอท
- `.github/workflows/availability-checker.yml` - การตั้งค่า GitHub Actions
- `requirements.txt` - Python dependencies
- `available_dates.json` - ผลลัพธ์การตรวจสอบล่าสุด (สร้างอัตโนมัติ)
- `notified_dates.json` - รายการวันที่ที่แจ้งเตือนแล้ว (สร้างอัตโนมัติ)

## 🔧 การทำงาน

1. GitHub Actions จะรันสคริปต์ทุก 5 นาที
2. ตรวจสอบช่องว่างจาก API
3. หากพบช่องว่างใหม่ จะส่งแจ้งเตือนไปยัง Discord
4. บันทึกผลลัพธ์ลงไฟล์ JSON
5. Commit การเปลี่ยนแปลงกลับไปยัง repository

## 📊 การตรวจสอบสถานะ

- ดูใน Actions tab เพื่อดูประวัติการรัน
- ดูไฟล์ `available_dates.json` เพื่อดูผลลัพธ์ล่าสุด
- ตรวจสอบ Discord channel เพื่อดูการแจ้งเตือน

## 🛑 การหยุดบอท

- ไปที่ Actions tab
- กด "Disable workflow" เพื่อหยุดการรันอัตโนมัติ

## 🔍 การ Debug

หากมีปัญหา:
1. ตรวจสอบ Actions logs
2. ตรวจสอบว่า Discord Webhook URL ถูกต้อง
3. ตรวจสอบว่า Secret ถูกตั้งค่าแล้ว
